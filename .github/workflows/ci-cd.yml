name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  cd:
    needs: [ci]
    uses: ./.github/workflows/deploy-gh-pages.yml
